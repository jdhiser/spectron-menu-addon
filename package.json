{
  "name": "spectron-menu-addon",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "files": ["dist"],
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:preload": "tsc -p tsconfig.preload.json",
    "test":
      "yarn build && yarn build:preload && mocha --reporter spec --require ts-node/register test/*test.ts && rm src/preload.js ",
    "lint": "tslint --config tslint.json -p tsconfig.json --format stylish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SALT-AND-PEPPER/spectron-menu-addon.git"
  },
  "keywords": ["spectron", "menu", "mock"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "electron": "^1.7.9",
    "spectron": "^3.7.2"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.44",
    "dts-bundle": "^0.7.3",
    "electron-prebuilt": "^1.4.13",
    "extract-text-webpack-plugin": "^3.0.2",
    "mkdirp": "^0.5.1",
    "mocha": "^4.0.1",
    "power-assert": "^1.4.4",
    "ts-loader": "^3.2.0",
    "ts-node": "^4.0.2",
    "tslint": "^5.8.0",
    "tslint-loader": "^3.5.3",
    "typescript": "^2.6.2",
    "webpack": "^3.10.0"
  }
}
